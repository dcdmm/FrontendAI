const version = APP_VERSION;

const msg = log("hello");

const user: User = {
    id: 1,
    name: "张三",
    email: "test@example.com"
};

const id: UserId = 123;

MyApp.initialize({ timeout: 3000, retries: 3 });


import { helper, Calculator } from 'my-library';
const message = helper("TypeScript");


// ******************************************************************

import type {User_m} from './f_types_modules'
