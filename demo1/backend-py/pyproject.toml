[project]
name = "backend-py"
version = "1.0.0"
description = "FastAPI + OpenAI SDK → 阿里 DashScope, 流式聊天后端"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn[standard]>=0.34.0",
    "openai>=1.59.3",
    "python-dotenv>=1.0.1",
]
